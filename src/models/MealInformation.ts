export interface MealInformation {
    mealTitle: string;
    mealDescription: string;
    mealPrice: string;
    mealId: number
  }

export interface MealCount {
  mealId: number,
  mealCount: number
}